# npm 12 defaults `allow-git` to "none", which blocks fetching git-referenced
# dependencies. homey-log (used for Sentry error reporting) depends on
# raven@github:athombv/raven-node, a transitive git dependency with no
# registry alternative, so "root" (direct deps only) isn't enough here.
allow-git=all
